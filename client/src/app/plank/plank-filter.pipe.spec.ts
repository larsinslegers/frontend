import { PlankFilterPipe } from './plank-filter.pipe';

describe('PlankFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PlankFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
